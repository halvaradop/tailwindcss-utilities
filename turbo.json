{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["src"],
      "outputs": ["dist"]
    },
    "dev": {
      "cache": false
    },
    "test": {
      "cache": true,
      "inputs": ["test"]
    },
    "test:watch": {
      "cache": false,
      "inputs": ["test"]
    }
  },
  "cacheDir": ".turbo/cache"
}
